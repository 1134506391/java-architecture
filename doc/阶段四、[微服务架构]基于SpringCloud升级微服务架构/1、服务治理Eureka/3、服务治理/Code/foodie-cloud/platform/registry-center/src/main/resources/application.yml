## 启动顺序： #1
spring:
  application:
    name: eureka-server
  profiles:
    active: dev

server:
  port: 20000

eureka:
  client:
#    拉取注册内容
    fetch-registry: false
#    自己注册自己
    register-with-eureka: false
  instance:
    ## 将localhost指向本机（host文件）
    hostname: localhost